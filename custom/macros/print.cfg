# =====================
# PRINT_START (Beacon-aware, KAMP-aware, no hard-coded units)
# =====================
[gcode_macro PRINT_START]
description: Printer-agnostic start with PREHEAT orchestration; Beacon probes at 150C
gcode:
  SAVE_GCODE_STATE NAME=STATE_PRINT_START

  {% set vars = printer["gcode_macro _PRINTER_VARS"] %}
  {% set bed = params.BED_TEMP|default(0)|float %}
  {% set hot = params.HOTEND_TEMP|default(0)|float %}
  {% set mat = params.MATERIAL|default(vars.current_material)|string|upper %}
  SET_GCODE_VARIABLE MACRO=_PRINTER_VARS VARIABLE=current_material VALUE="{mat}"

  {% set travel_f = (vars.travel_speed|float * 60)|int %}
  {% set z_f = (vars.safe_travel_z_speed|float * 60)|int %}

  _GET_BED_BOUNDS
  {% set max_z = printer.toolhead.axis_maximum.z|float %}
  {% set hop = [vars.z_hop_height|float, max_z - 2]|min %}

  RESET_MULTIPLIERS
  MAYBE_LOAD_SKEW_CORRECTION
  SET_GCODE_OFFSET Z=0 MOVE=0
  
  {% if "beacon" in printer.configfile.settings %}
    # Home XY so the mixer can move safely if enabled
    G28 X Y
    # Let PREHEAT orchestrate heating and (for high-temp) toolhead-assisted mixing at 150C
    PREHEAT BED_TEMP={bed} HOTEND_TEMP={printer["gcode_macro BEACON_VARS"].beacon_contact_calibration_temp|int} MATERIAL="{mat}" QUICK=0 TARGET_CHAMBER={params.TARGET_CHAMBER|default(vars.default_chamber_target)}

    # Beacon contact home + gantry leveling + optional mesh at 150C
    BEACON_CONTACT_HOME CALIBRATE=1
    {% if "quad_gantry_level" in printer.configfile.settings %}
      QUAD_GANTRY_LEVEL
    {% elif "z_tilt" in printer.configfile.settings %}
      Z_TILT_ADJUST
    {% elif "GANTRY_LEVEL" in printer.gcode.commands %}
      GANTRY_LEVEL
    {% else %}
      RESPOND MSG="No gantry leveling module detected; skipping."
    {% endif %}
    BEACON_CONTACT_HOME CALIBRATE=0
    {% if "bed_mesh" in printer.configfile.settings %}
      BEACON_MESH METHOD=contact
    {% endif %}

    # Final print temp and thermal expansion compensation
    M104 S{hot}
    M109 S{hot}
    G90
    G1 Z{hop} F{z_f}
    _BEACON_SET_NOZZLE_TEMP_OFFSET

  {% else %}
    # No Beacon: let PREHEAT drive either simple warmup or mixing based on MATERIAL/thresholds
    PREHEAT BED_TEMP={bed} HOTEND_TEMP={hot} MATERIAL="{mat}" QUICK=0 TARGET_CHAMBER={params.TARGET_CHAMBER|default(vars.default_chamber_target)}
    G28
    {% if "quad_gantry_level" in printer.configfile.settings %}
      QUAD_GANTRY_LEVEL
    {% elif "z_tilt" in printer.configfile.settings %}
      Z_TILT_ADJUST
    {% elif "GANTRY_LEVEL" in printer.gcode.commands %}
      GANTRY_LEVEL
    {% else %}
      RESPOND MSG="No gantry leveling module detected; skipping."
    {% endif %}
    {% if "bed_mesh" in printer.configfile.settings %}
      BED_MESH_CALIBRATE
    {% endif %}
  {% endif %}

  M117 Starting Print
  G90
  M83
  G92 E0

  # Purge strategy
  {% if "LINE_PURGE" in printer.gcode.commands %}
    LINE_PURGE
  {% elif "SMART_PURGE_LINE" in printer.gcode.commands %}
    SMART_PURGE_LINE LENGTH={vars.purge_line_length}
  {% else %}
    _FALLBACK_PURGE LENGTH={vars.purge_line_length} SPEED={vars.purge_speed}
  {% endif %}

  RESTORE_GCODE_STATE NAME=STATE_PRINT_START

# =====================
# PRINT_END (unit-safe; avoids duplicate M84; robust parking)
# =====================
[gcode_macro PRINT_END]
description: Consistent end-of-print routine
gcode:
  {% set reason = params.REASON|default("complete")|lower %}
  SAVE_GCODE_STATE NAME=STATE_PRINT_END
  {% set v = _PRINTER_VARS %}
  {% set f_xy = (v.safe_travel_xy_speed|float * 60)|int %}
  {% set f_z  = (v.safe_travel_z_speed|float  * 60)|int %}

  # Realtime bounds + park targets
  {% set min_x = printer.toolhead.axis_minimum.x|float %}
  {% set min_y = printer.toolhead.axis_minimum.y|float %}
  {% set max_x = printer.toolhead.axis_maximum.x|float %}
  {% set max_y = printer.toolhead.axis_maximum.y|float %}
  {% set margin = v.bed_margin|float %}
  {% set park_x = (max_x - margin) if (v.park_right|int == 1) else (min_x + margin) %}
  {% set park_y = (max_y - margin) if (v.park_back|int  == 1) else (min_y + margin) %}
  {% set max_z = printer.toolhead.axis_maximum.z|float %}
  {% set hop = [v.z_hop_height|float, max_z - 2]|min %}

  M400
  G92 E0
  G91
  G1 E-{v.end_retract_length|float} F{(v.retract_speed|float*60)|int}
  G90

  {% if "z" in printer.toolhead.homed_axes %}
    G1 Z{hop} F{z_f}
  {% endif %}

  {% if "x" in printer.toolhead.homed_axes and "y" in printer.toolhead.homed_axes %}
    G1 X{park_x} Y{park_y} F{travel_f}
  {% endif %}

  {% if "beacon" in printer.configfile.settings %}
    _BEACON_REMOVE_NOZZLE_TEMP_OFFSET
  {% endif %}

  # Clear mesh, cool down in a controlled way
  {% if "bed_mesh" in printer.configfile.settings %}
    BED_MESH_CLEAR
  {% endif %}

  M104 S0
  DISABLE_XY
  COOLDOWN_SEQUENCE QUICK=0
  RESET_MULTIPLIERS

  RESTORE_GCODE_STATE NAME=STATE_PRINT_END

	
[gcode_macro _PAUSE_STATE]
variable_prev_hotend_target: 0.0
variable_used_standby: 0
variable_park_x: 0.0
variable_park_y: 0.0
variable_was_homed: 0
gcode:

[gcode_macro PAUSE]
rename_existing: PAUSE_BASE
gcode:
  PAUSE_BASE
  {% set v   = printer["gcode_macro _PRINTER_VARS"] %}
  {% set fxy = (v.safe_travel_xy_speed|float * 60)|int %}
  {% set fz  = (v.safe_travel_z_speed|float  * 60)|int %}

  # Realtime bounds + park targets
  {% set min_x = printer.toolhead.axis_minimum.x|float %}
  {% set min_y = printer.toolhead.axis_minimum.y|float %}
  {% set max_x = printer.toolhead.axis_maximum.x|float %}
  {% set max_y = printer.toolhead.axis_maximum.y|float %}
  {% set margin = v.bed_margin|float %}
  {% set park_x = (max_x - margin) if (v.park_right|int == 1) else (min_x + margin) %}
  {% set park_y = (max_y - margin) if (v.park_back|int  == 1) else (min_y + margin) %}
  {% set max_z = printer.toolhead.axis_maximum.z|float %}
  {% set hop = [v.z_hop_height|float, max_z - 2]|min %}

  {% if "z" in printer.toolhead.homed_axes %}
    G91  ; Z-hop
    G1 Z{hop} F{fz}
    G90
  {% endif %}
  {% if "x" in printer.toolhead.homed_axes and "y" in printer.toolhead.homed_axes %}
    G90
    G1 X{park_x} Y{park_y} F{fxy}
  {% endif %}

  G91
  G1 E-{v.end_retract_length|float} F{(v.retract_speed|float*60)|int}
  G90

  M107  ; part cooling off; keep chamber/under-bed fans as-is

  # Hotend standby (drop, but not below pause_min_standby)
  {% set prev = printer.extruder.target|float %}
  {% set used = 0 %}
  {% if prev > 0 and v.pause_standby_delta|int > 0 %}
    {% set newt = [prev - v.pause_standby_delta|float, v.pause_min_standby|float]|max %}
    {% if newt < prev %}
      M104 S{newt}
      {% set used = 1 %}
    {% endif %}
  {% endif %}

  SET_GCODE_VARIABLE MACRO=_PAUSE_STATE VARIABLE=used_standby VALUE={used}
  SET_GCODE_VARIABLE MACRO=_PAUSE_STATE VARIABLE=prev_hotend_target VALUE={prev}
  SET_GCODE_VARIABLE MACRO=_PAUSE_STATE VARIABLE=park_x VALUE={park_x}
  SET_GCODE_VARIABLE MACRO=_PAUSE_STATE VARIABLE=park_y VALUE={park_y}
  SET_GCODE_VARIABLE MACRO=_PAUSE_STATE VARIABLE=was_homed VALUE={(1 if ("x" in printer.toolhead.homed_axes and "y" in printer.toolhead.homed_axes and "z" in printer.toolhead.homed_axes) else 0)}

[gcode_macro RESUME]
rename_existing: RESUME_BASE
gcode:
  {% set v = _PRINTER_VARS %}
  {% set used = printer["gcode_macro _PAUSE_STATE"].used_standby|int %}
  {% set prev = printer["gcode_macro _PAUSE_STATE"].prev_hotend_target|float %}
  {% if used == 1 and prev > 0 %} RESPOND MSG="Resuming: heating hotend back to {prev}C"; M109 S{prev} {% endif %}
  G91
  G1 E{v.end_retract_length|float} F{(v.retract_speed*60)|int}
  G90
  RESUME_BASE

[gcode_macro CANCEL_PRINT]
rename_existing: CANCEL_PRINT_BASE
gcode:
  PRINT_END REASON=cancel
  CANCEL_PRINT_BASE


[gcode_macro M600]
gcode: 
	SAVE_GCODE_STATE NAME=M600
	UNLOAD_FILAMENT
	SET_GCODE_VARIABLE MACRO=RESUME VARIABLE=m600 VALUE=1
	PAUSE
	RESTORE_GCODE_STATE NAME=M600